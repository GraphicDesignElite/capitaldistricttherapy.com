Run > Gulp. Enjoy!
Set Gulp Local version: 3.9.1 
Open .Dist folder in another window with live server to view changes and enjoy browser sync

npm install gulp-cli -g
npm install gulp -D
npx -p touch nodetouch gulpfile.js
gulp --help

Nunjucks - templates from pages are compiled to -> compiled, and then minified and moved into the dist folder.